import React from 'react';

const LogoButton = ({ children }) => {
    return (
        <div>
            <span className='koko-type-custom-font'>{children}</span>
        </div>  
    );
}

export default LogoButton;